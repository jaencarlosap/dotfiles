# Preferences (always in context — keep under 20 lines, one rule per line, English)

- Commits: never add Co-Authored-By / attribution trailers; the user rewrote history to remove them.
- Text the model reads (rules, skills, AGENTS.md, memory) is written in English; identifiers stay verbatim.
- Never run auth/login commands (mcporter auth, gh auth login) yourself: they open the user's browser and block; ask the user to run them
